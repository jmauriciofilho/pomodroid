<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="br.unifor.ads.pomodroid">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activity.SplashScreenActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activity.LoginActivity" />
        <activity
            android:name=".activity.UserFormActivity"
            android:label="@string/title_activity_user_form"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".activity.RegisterFormActivity" />
        <activity android:name=".activity.TesteActivity" />
        <activity android:name=".activity.TestTaskListActivity" />
        <activity android:name=".activity.TestTaskActivity" />
        <activity android:name=".activity.TestTagActivity" />
        <activity android:name=".activity.TestTasksTagsActivity" />
        <activity android:name=".activity.MainActivity" />
    </application>

</manifest>